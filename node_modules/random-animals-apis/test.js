const animalAPI = require("./index");
 
(async () => {
  // https://shibe.online/
  const shibaLink = await animalAPI.getRandomShibaInuImage()
  console.log(shibaLink)

  const birdLink = await animalAPI.getRandomBirdImage()
  console.log(birdLink)

  const catLink = await animalAPI.getRandomCatImage()
  console.log(catLink)

  // https://random.dog/
  const dogLink = await animalAPI.getRandomDogImage()
  console.log(dogLink)

  // https://randomfox.ca/
  const foxLink = await animalAPI.getRandomFoxImage()
  console.log(foxLink)
})()