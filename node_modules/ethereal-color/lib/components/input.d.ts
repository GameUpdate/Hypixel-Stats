import { InputForColor, PrivatePropertyColorState } from './color';
import { PropPaletteState, PropPaletteOptions, InputForPalette } from './palette';
import { PropGradientOptions, PropGradientState } from './gradient';
import { Palette } from './palette';
declare type ColorInput = {
    from: InputForColor;
};
declare type PaletteInput = {
    from: InputForPalette;
    options: PropPaletteOptions;
};
declare type GradientInput = {
    from: Palette;
    options: PropGradientOptions;
};
export declare type Input = {
    normalizeColor: (colorInAnyFormat: ColorInput) => PrivatePropertyColorState;
    normalizePalette: (baseColor: PaletteInput) => PropPaletteState;
    normalizeGradient: (basePalette: GradientInput) => PropGradientState;
};
export declare function Input(): Input;
export {};
