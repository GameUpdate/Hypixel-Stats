import { Palette } from './palette';
import { Color } from './color';
import { PropColorFormat } from './color';
export declare type PropGradientOptions = {
    count: number;
};
export declare type PropGradientState = Color[];
export declare type Gradient = {
    toStringArray: (format: PropColorFormat) => string[];
    toObjectArray: (format: PropColorFormat) => object[];
    toColorArray: () => Color[];
    set: (userInput: Palette, options?: PropGradientOptions) => void;
    random: (options?: PropGradientOptions) => void;
};
export declare function Gradient(userInput?: Palette, options?: PropGradientOptions): Gradient;
